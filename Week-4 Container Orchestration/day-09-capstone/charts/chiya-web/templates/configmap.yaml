apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
data:
  config.js: |
    window.CHIYA_CONFIG = {
      ordersApi: {{ .Values.config.ordersApi | quote }},
      kitchenApi: {{ .Values.config.kitchenApi | quote }},
      tagline: {{ .Values.config.tagline | quote }}
    };
