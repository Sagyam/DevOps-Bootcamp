<?xml version='1.1' encoding='UTF-8'?>
<flow-definition plugin="workflow-job">
  <description>ADVANCED: runs a stage inside an ephemeral node:22-alpine container. Requires Docker available to the controller.</description>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition" plugin="workflow-cps">
    <script>pipeline {
  agent none
  stages {
    stage('Node in a throwaway container') {
      agent { docker { image 'node:22-alpine' } }
      steps {
        sh 'node --version'
        sh 'echo "This stage ran inside an ephemeral node:22-alpine container, then it vanished."'
      }
    }
  }
}</script>
    <sandbox>true</sandbox>
  </definition>
  <triggers/>
  <disabled>false</disabled>
</flow-definition>
